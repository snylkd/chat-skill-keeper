// src/types/react-com-client.d.ts
declare module 'react-com/client' {
  // Si tu connais les types du module, tu peux les déclarer ici.
  // Par exemple :
  export function someFunction(arg: string): void;
  // Ou tu peux simplement déclarer le module de manière générique :
  const value: any;
  export = value;
}
